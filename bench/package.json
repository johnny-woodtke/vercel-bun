{
  "name": "bench",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "vercel:build": "bunx vercel build",
    "vercel:pull": "bunx vercel pull",
    "bench:all": "bun run src/run-all.ts",
    "bench:cold-start": "bun run src/cold-start.ts",
    "bench:warm": "k6 run src/warm-latency.ts",
    "bench:throughput": "k6 run src/throughput.ts",
    "bench:concurrency": "k6 run src/concurrency.ts",
    "bench:payload": "k6 run src/payload-size.ts",
    "bench:burst": "k6 run src/burst-traffic.ts",
    "bench:error": "k6 run src/error-handling.ts"
  },
  "dependencies": {
    "autocannon": "^7.12.0",
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "table": "^6.8.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/k6": "^1.0.2",
    "@types/node": "^22.15.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
